# SOTA RAG - Docker Compose Configuration
# For local development and testing

version: '3.8'

services:
  # Main RAG API service
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: sota-rag-api
    ports:
      - "8000:8000"
    environment:
      - MONGODB_URI=${MONGODB_URI}
      - VOYAGE_API_KEY=${VOYAGE_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - ENVIRONMENT=${ENVIRONMENT:-development}
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - sota-network
    volumes:
      # Mount for development hot reload (optional)
      - ../src:/app/src:ro

  # Optional: Local MongoDB for development
  # Uncomment if not using MongoDB Atlas
  # mongodb:
  #   image: mongo:7.0
  #   container_name: sota-mongodb
  #   ports:
  #     - "27017:27017"
  #   environment:
  #     - MONGO_INITDB_ROOT_USERNAME=admin
  #     - MONGO_INITDB_ROOT_PASSWORD=password
  #   volumes:
  #     - mongodb_data:/data/db
  #   networks:
  #     - sota-network

networks:
  sota-network:
    driver: bridge

# volumes:
#   mongodb_data:
